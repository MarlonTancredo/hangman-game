import styled from "styled-components";

import { colors, fontSize } from "../../styles/globalStyles";

export const WordDisplayWrapper = styled.div``;

export const Word = styled.p`
  color: ${colors.white};
  font-size: ${fontSize.small};
  font-weight: bold;
`;
